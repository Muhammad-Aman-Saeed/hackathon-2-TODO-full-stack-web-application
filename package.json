{
  "name": "hackathon-2-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install --legacy-peer-deps && npm run build"
  },
  "workspaces": [
    "frontend"
  ],
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "overrides": {
    "eslint": "$eslint"
  }
}